function log(message: string) {
    console.log(message);
}

log('Hello World TypeScript');

let test: Array<string> = ['Hello', 'World'];
let test2: [string] = ['other teste'];
let isActive: boolean = true;

/**
 * string
 * number
 * boolean
 * array
 * function
 * ...
 * Main Types (My Class)
 */